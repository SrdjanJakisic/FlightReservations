namespace FlightReservations.Models
{
    public enum City { Belgrade = 1, Nis = 2, Kraljevo = 3  }
    public enum FlightStatus { Active = 1, Cancelled = 2 }
    public enum ReservationStatus { Pending = 1, Approved = 2, Cancelled = 3 }
}
